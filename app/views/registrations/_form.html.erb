<%= form_with(model: registration, local: true) do |form| %>
  <% if registration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(registration.errors.count, "error") %> prohibited this registration from being saved:</h2>

      <ul>
      <% registration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :role %>
    <%= form.text_field :role, id: :registration_role %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.select(:user_id, User.all.map {|a| [a, a.id]}) %>
  </div>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.select(:course_id, Course.all.map {|a| [a, a.id]}) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
